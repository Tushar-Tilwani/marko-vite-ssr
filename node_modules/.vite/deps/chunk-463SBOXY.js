import {
  require_change_case
} from "./chunk-WTBMIWQS.js";
import {
  __commonJS
} from "./chunk-VUNV25KB.js";

// node_modules/marko/src/runtime/helpers/style-value.js
var require_style_value = __commonJS({
  "node_modules/marko/src/runtime/helpers/style-value.js"(exports, module) {
    var changeCase = require_change_case();
    module.exports = function styleHelper(style) {
      if (!style) {
        return;
      }
      var type = typeof style;
      if (type !== "string") {
        var styles = "";
        var sep = "";
        if (Array.isArray(style)) {
          for (var i = 0, len = style.length; i < len; i++) {
            var next = styleHelper(style[i]);
            if (next) {
              styles += sep + next;
              sep = ";";
            }
          }
        } else if (type === "object") {
          for (var name in style) {
            var value = style[name];
            if (value != null && value !== false) {
              if (typeof value === "number" && value) {
                value += "px";
              }
              styles += sep + changeCase.___camelToDashCase(name) + ":" + value;
              sep = ";";
            }
          }
        }
        return styles || void 0;
      }
      return style;
    };
  }
});

export {
  require_style_value
};
//# sourceMappingURL=chunk-463SBOXY.js.map
